# Core dependencies
requests>=2.28.0

# Django integration (optional - only needed for Django features)
django>=3.2

# Development/testing dependencies
pytest>=7.0.0
pytest-django>=4.5.0
responses>=0.23.0  # For mocking HTTP requests in tests
