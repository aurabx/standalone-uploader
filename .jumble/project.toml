[project]
name = "standalone-uploader"
description = "A standalone file uploader component for aurabox"

# [commands]
# build = "cargo build"
# test = "cargo test"
# lint = "cargo clippy"

# [entry_points]
# main = "src/main.rs"
